---
phase: 01-foundation-auth
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - apps/api/prisma/schema.prisma
  - apps/api/src/generated/prisma
  - apps/api/src/prisma/prisma.module.ts
  - apps/api/src/prisma/prisma.service.ts
  - apps/api/src/redis/redis.module.ts
  - apps/api/src/redis/redis.service.ts
  - apps/api/src/app.module.ts
autonomous: true
requirements:
  - FNDN-03
  - FNDN-04

must_haves:
  truths:
    - "Prisma schema defines users, boards, lists, cards, and refresh_tokens tables with correct types"
    - "Database tables are created in PostgreSQL via prisma db push"
    - "PrismaService is injectable anywhere in the NestJS app via @Global() module"
    - "RedisService connects to Redis and is injectable globally"
  artifacts:
    - path: "apps/api/prisma/schema.prisma"
      provides: "Full database schema with 5 models"
      contains: "generator client"
    - path: "apps/api/src/prisma/prisma.service.ts"
      provides: "PrismaClient wrapper with lifecycle hooks"
      contains: "class PrismaService"
    - path: "apps/api/src/prisma/prisma.module.ts"
      provides: "Global Prisma module"
      contains: "@Global()"
    - path: "apps/api/src/redis/redis.service.ts"
      provides: "ioredis wrapper"
      contains: "class RedisService"
  key_links:
    - from: "apps/api/src/app.module.ts"
      to: "apps/api/src/prisma/prisma.module.ts"
      via: "Module imports"
      pattern: "PrismaModule"
    - from: "apps/api/src/app.module.ts"
      to: "apps/api/src/redis/redis.module.ts"
      via: "Module imports"
      pattern: "RedisModule"
    - from: "apps/api/prisma/schema.prisma"
      to: "apps/api/src/generated/prisma"
      via: "Prisma generate output"
      pattern: 'output.*generated/prisma'
---

<objective>
Create the Prisma 7 database schema with all Phase 1 tables (users, boards, lists, cards, refresh_tokens), push to PostgreSQL, and set up global PrismaModule and RedisModule for NestJS dependency injection.

Purpose: Database and cache layer must exist before auth or WebSocket features can be built.
Output: Working Prisma schema, generated client, PrismaModule and RedisModule available globally.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-auth/01-CONTEXT.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Prisma 7 schema with all models, indexes, and push to database</name>
  <files>apps/api/prisma/schema.prisma</files>
  <read_first>
    .planning/phases/01-foundation-auth/01-RESEARCH.md (Code Examples: Prisma 7 Schema section — full schema with all models, Pitfall 3: custom output, Pitfall 6: generator name)
    DESIGN.md (color palette hex values for bot user colors)
  </read_first>
  <action>
**Per D-14, D-15:** Create `apps/api/prisma/schema.prisma` with exactly the following:

**Generator config (CRITICAL — per Pitfall 6):**
```prisma
generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
```
- MUST use `prisma-client` NOT `prisma-client-js` (Prisma 7 breaking change)
- MUST use custom output `../src/generated/prisma` (Pitfall 3 — monorepo hoisting fix)

**Models — exactly 5 tables per D-14:**

1. **User** (`@@map("users")`): `id` String @id @default(uuid()), `email` String @unique, `passwordHash` String @map("password_hash"), `name` String, `avatarUrl` String? @map("avatar_url"), `color` String, `isBot` Boolean @default(false) @map("is_bot"), `createdAt` DateTime @default(now()) @map("created_at"). Relations: boards Board[], cards Card[], refreshTokens RefreshToken[].

2. **Board** (`@@map("boards")`): `id` String @id @default(uuid()), `name` String, `description` String?, `backgroundColor` String? @map("background_color"), `isDemo` Boolean @default(false) @map("is_demo"), `createdById` String @map("created_by"), `createdBy` User relation. `createdAt` DateTime @default(now()) @map("created_at"). Relations: lists List[]. Index: `@@index([isDemo], name: "idx_boards_demo")`.

3. **List** (`@@map("lists")`): `id` String @id @default(uuid()), `boardId` String @map("board_id"), `board` Board relation (onDelete: Cascade), `name` String, `position` Float, `createdAt` DateTime @default(now()) @map("created_at"). Relations: cards Card[]. Index: `@@index([boardId, position])`.

4. **Card** (`@@map("cards")`): `id` String @id @default(uuid()), `listId` String @map("list_id"), `list` List relation (onDelete: Cascade), `title` String, `descriptionText` String? @map("description_text"), `descriptionYjs` Bytes? @map("description_yjs"), `position` Float, `coverColor` String? @map("cover_color"), `createdById` String @map("created_by"), `createdBy` User relation, `createdAt` DateTime @default(now()) @map("created_at"), `updatedAt` DateTime @updatedAt @map("updated_at"). Index: `@@index([listId, position])`.

5. **RefreshToken** (`@@map("refresh_tokens")`): `id` String @id @default(uuid()), `token` String @unique, `userId` String @map("user_id"), `user` User relation (onDelete: Cascade), `revoked` Boolean @default(false), `expiresAt` DateTime @map("expires_at"), `createdAt` DateTime @default(now()) @map("created_at"). Index: `@@index([userId])`.

**After creating schema:**
1. Create `.env` file in `apps/api/` with `DATABASE_URL=postgresql://flowboard:flowboard_dev@localhost:5432/flowboard`
2. Ensure Docker Compose is running: `docker compose up -d`
3. Run `npx prisma generate` from `apps/api/` to generate the client to `src/generated/prisma`
4. Run `npx prisma db push` from `apps/api/` to create tables in PostgreSQL

**Note:** D-15 mentions a partial index `WHERE is_demo = true`. Prisma 7 may not support partial indexes declaratively. If `@@index` doesn't support `where`, defer the partial index to a raw SQL migration later — the non-partial `@@index([isDemo])` is sufficient for Phase 1.
  </action>
  <verify>
    <automated>cd /home/camailo/Documents/flowboard/apps/api && npx prisma validate 2>&1 && echo "SCHEMA_VALID"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/prisma/schema.prisma` contains `provider = "prisma-client"` (NOT `prisma-client-js`)
    - `apps/api/prisma/schema.prisma` contains `output   = "../src/generated/prisma"`
    - `apps/api/prisma/schema.prisma` contains `model User` and `model Board` and `model List` and `model Card` and `model RefreshToken`
    - `apps/api/prisma/schema.prisma` contains `@@map("users")` and `@@map("boards")` and `@@map("lists")` and `@@map("cards")` and `@@map("refresh_tokens")`
    - `apps/api/prisma/schema.prisma` contains `descriptionYjs  Bytes?` with `@map("description_yjs")`
    - `apps/api/prisma/schema.prisma` contains `position  Float` in both List and Card models
    - `apps/api/prisma/schema.prisma` contains `@@index([boardId, position])` and `@@index([listId, position])` and `@@index([userId])`
    - `npx prisma validate` exits with code 0
    - Directory `apps/api/src/generated/prisma` exists after `prisma generate`
  </acceptance_criteria>
  <done>Prisma schema validates. All 5 models created with correct types, indexes, and mapped table names. Client generated to custom output path.</done>
</task>

<task type="auto">
  <name>Task 2: Create global PrismaModule and RedisModule for NestJS DI</name>
  <files>
    apps/api/src/prisma/prisma.module.ts, apps/api/src/prisma/prisma.service.ts,
    apps/api/src/redis/redis.module.ts, apps/api/src/redis/redis.service.ts,
    apps/api/src/app.module.ts
  </files>
  <read_first>
    .planning/phases/01-foundation-auth/01-RESEARCH.md (Pattern 3: NestJS Global Module PrismaModule)
    apps/api/src/app.module.ts (current state after Plan 01)
    apps/api/prisma/schema.prisma (verify output path for import)
  </read_first>
  <action>
**PrismaService** — per RESEARCH.md Pattern 3:

Create `apps/api/src/prisma/prisma.service.ts`:
```typescript
import { Injectable, OnModuleInit, OnModuleDestroy } from '@nestjs/common';
import { PrismaClient } from '../generated/prisma';

@Injectable()
export class PrismaService extends PrismaClient implements OnModuleInit, OnModuleDestroy {
  async onModuleInit() {
    await this.$connect();
  }

  async onModuleDestroy() {
    await this.$disconnect();
  }
}
```
- Import PrismaClient from `../generated/prisma` (custom output path per Pitfall 3)

**PrismaModule** — global module:

Create `apps/api/src/prisma/prisma.module.ts`:
```typescript
import { Global, Module } from '@nestjs/common';
import { PrismaService } from './prisma.service';

@Global()
@Module({
  providers: [PrismaService],
  exports: [PrismaService],
})
export class PrismaModule {}
```

**RedisService** — ioredis wrapper:

Create `apps/api/src/redis/redis.service.ts`:
```typescript
import { Injectable, OnModuleDestroy } from '@nestjs/common';
import { ConfigService } from '@nestjs/config';
import Redis from 'ioredis';

@Injectable()
export class RedisService extends Redis implements OnModuleDestroy {
  constructor(configService: ConfigService) {
    super(configService.get<string>('REDIS_URL') || 'redis://localhost:6379');
  }

  async onModuleDestroy() {
    await this.quit();
  }
}
```

**RedisModule** — global module:

Create `apps/api/src/redis/redis.module.ts`:
```typescript
import { Global, Module } from '@nestjs/common';
import { RedisService } from './redis.service';

@Global()
@Module({
  providers: [RedisService],
  exports: [RedisService],
})
export class RedisModule {}
```

**Update `apps/api/src/app.module.ts`:**
Add `PrismaModule` and `RedisModule` to imports array (alongside existing `ConfigModule.forRoot({ isGlobal: true })`).

**Update `apps/api/.env`** (if not already present):
Add `REDIS_URL=redis://localhost:6379`
  </action>
  <verify>
    <automated>cd /home/camailo/Documents/flowboard/apps/api && npx ts-node -e "import('./src/prisma/prisma.service').then(() => console.log('PRISMA_SERVICE_OK'))" 2>&1 || echo "Check TypeScript compilation" && ls src/prisma/prisma.module.ts src/prisma/prisma.service.ts src/redis/redis.module.ts src/redis/redis.service.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/prisma/prisma.service.ts` contains `import { PrismaClient } from '../generated/prisma'` (NOT from `@prisma/client`)
    - `apps/api/src/prisma/prisma.service.ts` contains `class PrismaService extends PrismaClient`
    - `apps/api/src/prisma/prisma.module.ts` contains `@Global()` decorator
    - `apps/api/src/prisma/prisma.module.ts` contains `exports: [PrismaService]`
    - `apps/api/src/redis/redis.service.ts` contains `class RedisService extends Redis`
    - `apps/api/src/redis/redis.service.ts` contains `configService.get<string>('REDIS_URL')`
    - `apps/api/src/redis/redis.module.ts` contains `@Global()` decorator
    - `apps/api/src/app.module.ts` contains `PrismaModule` and `RedisModule` in imports
  </acceptance_criteria>
  <done>PrismaModule and RedisModule are global NestJS modules. PrismaService imports from custom generated path. RedisService reads URL from ConfigService.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Push database schema to PostgreSQL</name>
  <files>apps/api/prisma/schema.prisma</files>
  <read_first>
    apps/api/prisma/schema.prisma (verify schema is valid before push)
    apps/api/.env (verify DATABASE_URL is set)
  </read_first>
  <action>
**[BLOCKING] This task MUST complete before any verification that depends on database tables.**

1. Ensure Docker Compose is running: `docker compose up -d` from project root
2. Wait for PostgreSQL to be ready: `docker compose exec postgres pg_isready -U flowboard`
3. From `apps/api/`, run: `npx prisma db push`
   - If there's a TTY issue, use: `npx prisma db push --accept-data-loss` (safe on empty database)
4. Verify tables exist: `docker compose exec postgres psql -U flowboard -d flowboard -c '\dt'`
   - Expected tables: users, boards, lists, cards, refresh_tokens
5. Run `npx prisma generate` if not already done (ensure generated client is up to date)
  </action>
  <verify>
    <automated>cd /home/camailo/Documents/flowboard && docker compose exec postgres psql -U flowboard -d flowboard -c '\dt' 2>&1 | grep -c 'users\|boards\|lists\|cards\|refresh_tokens'</automated>
  </verify>
  <acceptance_criteria>
    - `npx prisma db push` exits with code 0
    - PostgreSQL contains 5 tables: users, boards, lists, cards, refresh_tokens
    - `apps/api/src/generated/prisma/` directory exists with generated client files
  </acceptance_criteria>
  <done>Database schema pushed to PostgreSQL. All 5 tables created. Prisma client generated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → PostgreSQL | Database credentials in .env, not committed to git |
| App → Redis | Redis on localhost, no auth (local dev only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | I (Information Disclosure) | Database credentials | mitigate | .env is gitignored; .env.example has placeholder values |
| T-01-02-02 | T (Tampering) | Prisma generated client | accept | Generated to src/generated/prisma, gitignored; regenerated on install |
</threat_model>

<verification>
1. `npx prisma validate` passes
2. `npx prisma db push` creates tables in PostgreSQL
3. All 5 models reflected in database
4. PrismaModule and RedisModule are global and exported
5. NestJS app starts without errors with both modules loaded
</verification>

<success_criteria>
Database schema complete with all Phase 1 tables. Prisma client generated to custom output path. PrismaModule and RedisModule globally available in NestJS. Database tables created in PostgreSQL.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-02-SUMMARY.md`
</output>
