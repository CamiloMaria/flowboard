---
phase: 03-real-time-collaboration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/presence.types.ts
  - packages/shared/src/ws-events.types.ts
  - packages/shared/src/index.ts
autonomous: true
requirements:
  - PRES-03
  - PRES-06
  - COLLAB-03
must_haves:
  truths:
    - "Presence and collaboration types are importable from @flowboard/shared"
    - "WsEventMap includes typed payloads for presence:cursor, presence:join, presence:leave"
  artifacts:
    - path: "packages/shared/src/presence.types.ts"
      provides: "Presence type definitions"
      exports: ["OnlineUser", "CursorPosition", "PresenceCursorPayload", "PresenceJoinPayload", "PresenceLeavePayload", "CoEditorInfo"]
    - path: "packages/shared/src/ws-events.types.ts"
      provides: "Updated WsEventMap with presence payloads"
      contains: "PresenceCursorPayload"
  key_links:
    - from: "packages/shared/src/ws-events.types.ts"
      to: "packages/shared/src/presence.types.ts"
      via: "import type"
      pattern: "import.*PresenceCursorPayload"
---

<objective>
Define shared TypeScript types for the presence and collaboration systems so that backend (Plan 02, Plan 04) and frontend (Plan 03, Plan 04) plans can import typed contracts from @flowboard/shared.

Purpose: Establish interface contracts before implementation — prevents scavenger hunt anti-pattern.
Output: presence.types.ts with all presence/collab interfaces, updated WsEventMap.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-real-time-collaboration/03-CONTEXT.md
@.planning/phases/03-real-time-collaboration/03-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From packages/shared/src/auth.types.ts:
```typescript
export interface UserPayload {
  sub: string;
  email?: string;
  name: string;
  color: string;
  role: 'user' | 'guest';
}
```

From packages/shared/src/ws-events.types.ts:
```typescript
export type WsEventType =
  | 'board:join' | 'board:leave'
  | 'card:move' | 'card:create' | 'card:update' | 'card:delete'
  | 'list:create' | 'list:update' | 'list:delete'
  | 'presence:cursor' | 'presence:join' | 'presence:leave';

export interface WsEventMap {
  'board:join': { boardId: string };
  'board:leave': { boardId: string };
  'card:move': CardMovePayload;
  // ... (presence entries are missing typed payloads)
}
```

From packages/shared/src/index.ts:
```typescript
export type { UserPayload, TokenResponse, RegisterDto, LoginDto } from './auth.types';
export type { Board, List, Card, BoardWithLists, ... } from './board.types';
export type { WsEventType, WsEventMap } from './ws-events.types';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create presence type definitions</name>
  <files>packages/shared/src/presence.types.ts</files>
  <read_first>
    - packages/shared/src/auth.types.ts
    - packages/shared/src/board.types.ts
    - packages/shared/src/ws-events.types.ts
    - packages/shared/src/index.ts
  </read_first>
  <action>
Create `packages/shared/src/presence.types.ts` with the following interfaces. These types are the contracts for all presence and collaboration features in Phase 3.

```typescript
/** A user currently online on a board */
export interface OnlineUser {
  userId: string;
  name: string;
  color: string;  // hex from USER_COLORS/BOT_COLORS/GUEST_COLORS
  role: 'user' | 'guest';
}

/** Remote cursor position on the board canvas */
export interface CursorPosition {
  x: number;      // relative to board canvas scroll container
  y: number;
  lastUpdate: number; // Date.now() timestamp for idle detection
}

/** Socket.io payload for presence:cursor events (D-21) */
export interface PresenceCursorPayload {
  userId: string;
  name: string;
  color: string;
  x: number;
  y: number;
  boardId: string;
}

/** Socket.io payload for presence:join events (D-22) */
export interface PresenceJoinPayload {
  user: OnlineUser;
  boardId: string;
}

/** Socket.io payload for presence:leave events (D-22) */
export interface PresenceLeavePayload {
  userId: string;
  boardId: string;
}

/** Info about a co-editor in a card modal (D-14) */
export interface CoEditorInfo {
  userId: string;
  name: string;
  color: string;
}
```
  </action>
  <acceptance_criteria>
    - packages/shared/src/presence.types.ts exists
    - File exports OnlineUser, CursorPosition, PresenceCursorPayload, PresenceJoinPayload, PresenceLeavePayload, CoEditorInfo
    - OnlineUser contains userId, name, color, role fields
    - PresenceCursorPayload contains x, y, boardId, userId, name, color fields
  </acceptance_criteria>
  <verify>
    <automated>cd packages/shared && npx tsc --noEmit</automated>
  </verify>
  <done>presence.types.ts compiles and exports all 6 interfaces</done>
</task>

<task type="auto">
  <name>Task 2: Update WsEventMap with presence payloads and re-export</name>
  <files>packages/shared/src/ws-events.types.ts, packages/shared/src/index.ts</files>
  <read_first>
    - packages/shared/src/ws-events.types.ts
    - packages/shared/src/index.ts
    - packages/shared/src/presence.types.ts
  </read_first>
  <action>
1. Update `packages/shared/src/ws-events.types.ts`:
   - Add import: `import type { PresenceCursorPayload, PresenceJoinPayload, PresenceLeavePayload } from './presence.types';`
   - Add typed entries to WsEventMap:
     ```typescript
     'presence:cursor': PresenceCursorPayload;
     'presence:join': PresenceJoinPayload;
     'presence:leave': PresenceLeavePayload;
     ```

2. Update `packages/shared/src/index.ts`:
   - Add re-export: `export type { OnlineUser, CursorPosition, PresenceCursorPayload, PresenceJoinPayload, PresenceLeavePayload, CoEditorInfo } from './presence.types';`
  </action>
  <acceptance_criteria>
    - ws-events.types.ts imports PresenceCursorPayload, PresenceJoinPayload, PresenceLeavePayload from './presence.types'
    - WsEventMap has 'presence:cursor': PresenceCursorPayload entry
    - WsEventMap has 'presence:join': PresenceJoinPayload entry
    - WsEventMap has 'presence:leave': PresenceLeavePayload entry
    - index.ts re-exports OnlineUser, CursorPosition, PresenceCursorPayload, PresenceJoinPayload, PresenceLeavePayload, CoEditorInfo
  </acceptance_criteria>
  <verify>
    <automated>cd packages/shared && npx tsc --noEmit</automated>
  </verify>
  <done>WsEventMap is fully typed for presence events; all types importable from @flowboard/shared</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan creates type definitions only — no runtime code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | N/A | Types only | accept | No runtime code — no attack surface |
</threat_model>

<verification>
- `cd packages/shared && npx tsc --noEmit` passes
- All 6 presence types importable from @flowboard/shared
- WsEventMap includes typed presence entries
</verification>

<success_criteria>
- Shared types compile without errors
- downstream plans (02, 03, 04) can import typed presence interfaces
</success_criteria>

<output>
After completion, create `.planning/phases/03-real-time-collaboration/03-01-SUMMARY.md`
</output>
