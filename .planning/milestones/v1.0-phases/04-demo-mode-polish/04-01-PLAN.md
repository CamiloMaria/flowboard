---
phase: 04-demo-mode-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/board/CardItem.tsx
  - apps/web/src/components/board/CardDragOverlay.tsx
  - apps/web/src/components/presence/RemoteCursor.tsx
  - apps/web/src/components/presence/CursorGhostTrace.tsx
  - apps/web/src/components/board/ColumnContainer.tsx
  - apps/web/src/app.css
autonomous: true
requirements: [PLSH-01, PLSH-02, PLSH-03, PLSH-04]

must_haves:
  truths:
    - "Dragged card tilts +/-2deg based on horizontal velocity during drag"
    - "When a remote cursor disappears, a glow dot lingers for 600ms at last position"
    - "When a card is added/removed from a column, sibling cards stagger their layout shift with 30ms offset"
    - "Modal open/close uses spring scale + fade animation"
  artifacts:
    - path: "apps/web/src/components/presence/CursorGhostTrace.tsx"
      provides: "Ghost trace glow element that persists after cursor unmounts"
      min_lines: 20
    - path: "apps/web/src/components/board/CardItem.tsx"
      provides: "Updated card with stagger delay support"
    - path: "apps/web/src/components/board/CardDragOverlay.tsx"
      provides: "Updated drag overlay with velocity-based rotation"
  key_links:
    - from: "apps/web/src/components/presence/CursorGhostTrace.tsx"
      to: "apps/web/src/components/presence/RemoteCursor.tsx"
      via: "AnimatePresence exit triggers ghost trace spawn"
      pattern: "AnimatePresence.*CursorGhostTrace"
    - from: "apps/web/src/components/board/CardItem.tsx"
      to: "apps/web/src/components/board/ColumnContainer.tsx"
      via: "stagger index prop passed to layout transition delay"
      pattern: "staggerIndex.*30"
---

<objective>
Implement animation refinements per DESIGN.md: card drag rotation, cursor ghost trace, and column card cascade stagger.

Purpose: These animations are the premium interactions that make FlowBoard feel physically grounded. Drag rotation adds weight, ghost traces sell "these are real people," and cascade stagger makes card movements feel alive — all critical during bot choreography.
Output: Enhanced CardItem, CardDragOverlay, RemoteCursor, and new CursorGhostTrace component.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@DESIGN.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From apps/web/src/components/board/CardItem.tsx:
```typescript
interface CardItemProps {
  card: Card;
  boardId: string;
  index: number;
  onClick: () => void;
}

const layoutTransition = {
  type: 'spring' as const,
  stiffness: 200,
  damping: 25,
  mass: 0.8,
};
```

From apps/web/src/components/board/CardDragOverlay.tsx:
```typescript
// Currently uses static rotation: rotate-2deg
// Needs velocity-based dynamic rotation
```

From apps/web/src/components/presence/RemoteCursor.tsx:
```typescript
interface RemoteCursorProps {
  userId: string;
  name: string;
  color: string;
  position: CursorPosition;
}
// Exit animation: opacity: 0, scale: 0.8, duration: 0.4s
// Ghost trace NOT yet implemented
```

From apps/web/src/hooks/useReducedMotion.ts:
```typescript
export function useReducedMotion(): boolean;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Card drag rotation + modal spring animations</name>
  <files>apps/web/src/components/board/CardDragOverlay.tsx, apps/web/src/components/board/CardDetailModal.tsx</files>
  <read_first>
    - apps/web/src/components/board/CardDragOverlay.tsx
    - apps/web/src/components/board/CardDetailModal.tsx
    - apps/web/src/hooks/useReducedMotion.ts
    - DESIGN.md (lines 286-330 for animation specs)
  </read_first>
  <behavior>
    - Test 1: CardDragOverlay renders with rotation style when velocity data provided
    - Test 2: Rotation is clamped to +/-2deg range
    - Test 3: Reduced motion disables rotation
  </behavior>
  <action>
    Update CardDragOverlay to accept velocity-based rotation per D-08:
    - Add `dragVelocity` prop (number, horizontal pixels/sec from @dnd-kit drag event)
    - Compute rotation: `clamp(velocity * 0.01, -2, 2)` degrees — positive velocity = clockwise tilt
    - Apply via `style={{ transform: \`rotate(${rotation}deg)\` }}` on the overlay wrapper
    - Use spring transition per DESIGN.md: `spring(1, 80, 12)` = `{ type: 'spring', stiffness: 80, damping: 12, mass: 1 }`
    - Add 40ms lag via motion's `transition.x/y` with type: 'tween', duration: 0.04
    - When `useReducedMotion()` is true, set rotation to 0

    Update CardDetailModal open/close animations per PLSH-03:
    - Open: `initial={{ opacity: 0, scale: 0.95 }}`, `animate={{ opacity: 1, scale: 1 }}`, transition `{ type: 'spring', stiffness: 300, damping: 25, mass: 0.8 }` (ease-spring mapped to spring)
    - Close: `exit={{ opacity: 0, scale: 0.97 }}`, transition `{ duration: 0.2, ease: [0.65, 0, 0.35, 1] }` (ease-in)

    Wire velocity in BoardCanvas.tsx: capture `event.operation.velocity?.x` from @dnd-kit onDragOver/onDragStart and pass to CardDragOverlay. Store in a ref.
  </action>
  <acceptance_criteria>
    - CardDragOverlay.tsx contains `rotation` or `rotate` variable computed from velocity
    - CardDragOverlay.tsx contains `Math.max(-2, Math.min(2,` or equivalent clamp
    - CardDragOverlay.tsx contains `stiffness: 80` and `damping: 12`
    - CardDetailModal.tsx contains `scale: 0.95` in initial and `scale: 0.97` in exit
    - useReducedMotion check present in CardDragOverlay
  </acceptance_criteria>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Card drag overlay tilts +/-2deg based on horizontal velocity with spring(1,80,12), modal uses spring open/ease-in close, reduced motion respected</done>
</task>

<task type="auto">
  <name>Task 2: Cursor ghost trace + column cascade stagger</name>
  <files>apps/web/src/components/presence/CursorGhostTrace.tsx, apps/web/src/components/presence/RemoteCursor.tsx, apps/web/src/components/presence/CursorOverlay.tsx, apps/web/src/components/board/CardItem.tsx, apps/web/src/components/board/ColumnContainer.tsx, apps/web/src/app.css</files>
  <read_first>
    - apps/web/src/components/presence/RemoteCursor.tsx
    - apps/web/src/components/presence/CursorOverlay.tsx
    - apps/web/src/components/board/CardItem.tsx
    - apps/web/src/components/board/ColumnContainer.tsx
    - apps/web/src/app.css
    - DESIGN.md (lines 296-298 for ghost trace and cascade specs)
  </read_first>
  <action>
    **Cursor Ghost Trace (D-09, PLSH-04):**
    Create `CursorGhostTrace.tsx` — a standalone glow-dot component rendered via AnimatePresence when a cursor exits:
    - Props: `{ color: string; x: number; y: number }` — last known position of the departing cursor
    - Renders a 14px circle with `background: {color}`, `filter: drop-shadow(0 0 14px {color})`, `border-radius: 50%`
    - Uses motion.div with `initial={{ opacity: 0.8, scale: 1 }}`, `animate={{ opacity: 0, scale: 0.5 }}`, `transition={{ duration: 0.6, ease: 'easeOut' }}`
    - After the 600ms animation completes, the element self-removes via `onAnimationComplete` calling a parent callback or AnimatePresence handling

    Update `CursorOverlay.tsx`:
    - Maintain a `ghostTraces` state: `Map<string, { color: string; x: number; y: number }>` (keyed by userId)
    - When a cursor disappears from the presence store (user leaves), capture their last position into `ghostTraces`
    - Render ghost traces via AnimatePresence alongside live cursors
    - Ghost trace removes itself after animation completes (onAnimationComplete removes from map)

    Update `RemoteCursor.tsx`:
    - Phase 1 exit (already exists): cursor arrow + name label fade out over 400ms — keep existing exit animation
    - Phase 2 (new): ghost trace is separate component rendered by CursorOverlay, not part of RemoteCursor

    **Column Card Cascade Stagger (D-10):**
    Update `CardItem.tsx`:
    - Add `staggerIndex` prop (number) — the card's index within its column
    - Modify `layoutTransition` to include `delay: staggerIndex * 0.03` (30ms per card)
    - The stagger fires on layout shifts (when a card is added/removed from the column, sibling cards animate their position change with the stagger offset)
    - Total max stagger for 17 cards: 510ms, but per-column max ~5 cards = 150ms which is within the 200ms target
    - When `useReducedMotion()` is true, set delay to 0

    Update `ColumnContainer.tsx`:
    - Pass the `index` as `staggerIndex` prop when rendering CardItem (already has index from sorted card order)

    **Card create/archive animations (PLSH-02):**
    Ensure CardItem has AnimatePresence-compatible enter/exit:
    - Create: `initial={{ opacity: 0, scale: 0.95 }}`, `animate={{ opacity: 1, scale: 1 }}`, transition 250ms ease-out
    - Archive/delete: `exit={{ opacity: 0, scale: 0.95 }}`, transition 300ms ease-in
    - These may already exist via the AnimatePresence in ColumnContainer — verify and enhance if needed

    Update `app.css` if ghost trace needs any keyframe animations.
  </action>
  <acceptance_criteria>
    - CursorGhostTrace.tsx exists and exports a component
    - CursorGhostTrace.tsx contains `duration: 0.6` and `opacity: 0`
    - CursorGhostTrace.tsx contains `drop-shadow` or `boxShadow` with color
    - CursorOverlay.tsx contains `ghostTraces` or `ghostTrace` state management
    - CardItem.tsx contains `staggerIndex` prop in interface
    - CardItem.tsx contains `delay:` computed from staggerIndex
    - CardItem.tsx contains `scale: 0.95` in initial or exit for create/archive animation
    - ColumnContainer.tsx passes staggerIndex to CardItem
  </acceptance_criteria>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Ghost trace glow dot lingers 600ms after cursor exit, column cards stagger 30ms per card on layout shift, card create/archive has fade+scale animation, all reduced-motion safe</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan is frontend-only animation work with no trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | D (DoS) | CursorGhostTrace | accept | Ghost traces are short-lived (600ms) and only created on user disconnect — no amplification vector. Map is bounded by max online users (~10) |
| T-04-02 | D (DoS) | Cascade stagger | accept | Stagger delay is bounded by card count per column (~5). No user-controlled input to stagger timing |
</threat_model>

<verification>
- `cd apps/web && npx tsc --noEmit` — zero TypeScript errors
- Drag a card horizontally fast — should tilt slightly
- Disconnect a remote cursor — should see glow linger for ~600ms
- Add a card to a column with other cards — siblings should ripple into new positions
</verification>

<success_criteria>
- Card drag overlay rotates +/-2deg based on velocity with spring(1,80,12) and 40ms lag
- Remote cursor exit produces two-phase animation: fade out (400ms) + glow linger (600ms)
- Column cards stagger layout shifts at 30ms per card offset
- Card create uses fade+scale in (250ms), card archive uses fade+scale out (300ms)
- Modal open uses spring scale, close uses ease-in scale
- All animations respect useReducedMotion
</success_criteria>

<output>
After completion, create `.planning/phases/04-demo-mode-polish/04-01-SUMMARY.md`
</output>
