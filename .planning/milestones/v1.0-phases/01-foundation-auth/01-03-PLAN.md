---
phase: 01-foundation-auth
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - apps/web/src/App.tsx
  - apps/web/src/app.css
  - apps/web/src/pages/DemoPage.tsx
  - apps/web/src/pages/HomePage.tsx
  - apps/web/src/pages/LoginPage.tsx
  - apps/web/src/pages/RegisterPage.tsx
  - apps/web/src/lib/api.ts
autonomous: true
requirements:
  - FNDN-02

must_haves:
  truths:
    - "Vite dev server starts on port 5173 with hot reload"
    - "React Router routes work: / (home), /demo, /login, /register"
    - "Tailwind CSS v4 classes render correctly in the browser"
    - "API helper function configured to call /api/* endpoints"
  artifacts:
    - path: "apps/web/src/App.tsx"
      provides: "Root component with React Router setup"
      contains: "BrowserRouter"
    - path: "apps/web/src/pages/DemoPage.tsx"
      provides: "Demo page placeholder for /demo route"
    - path: "apps/web/src/lib/api.ts"
      provides: "Fetch wrapper for API calls"
  key_links:
    - from: "apps/web/src/App.tsx"
      to: "apps/web/src/pages/DemoPage.tsx"
      via: "React Router route"
      pattern: "path.*demo"
---

<objective>
Create the React frontend shell with Tailwind CSS v4, React Router routes for all Phase 1 pages (/demo, /login, /register), and an API helper utility.

Purpose: Frontend must exist for the dual WebSocket spike to validate client connections, and for auth flow testing.
Output: Working Vite React app with routing and Tailwind styling, ready to consume API endpoints.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-auth/01-CONTEXT.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up React Router routes, Tailwind v4 CSS, and page shells</name>
  <files>
    apps/web/src/App.tsx, apps/web/src/app.css,
    apps/web/src/pages/DemoPage.tsx, apps/web/src/pages/HomePage.tsx,
    apps/web/src/pages/LoginPage.tsx, apps/web/src/pages/RegisterPage.tsx
  </files>
  <read_first>
    apps/web/src/App.tsx (current state from Plan 01)
    apps/web/src/app.css (verify Tailwind import)
    apps/web/vite.config.ts (verify proxy config)
    DESIGN.md (dark theme colors: background, text, accent)
  </read_first>
  <action>
**Per D-03 and RESEARCH.md Tailwind v4 gotcha:**

1. **`apps/web/src/app.css`** — Tailwind v4 CSS-first config (NO tailwind.config.js):
```css
@import "tailwindcss";

@theme {
  --color-surface-primary: #0B0E14;
  --color-surface-secondary: #121620;
  --color-surface-tertiary: #1A1F2E;
  --color-text-primary: #E8ECF4;
  --color-text-secondary: #8B93A6;
  --color-accent: #22D3EE;
  --color-accent-hover: #06B6D4;
  --font-display: 'Space Grotesk', sans-serif;
  --font-body: 'DM Sans', sans-serif;
}
```

2. **`apps/web/src/App.tsx`** — React Router with routes:
```tsx
import { BrowserRouter, Routes, Route } from 'react-router';
import './app.css';
import { HomePage } from './pages/HomePage';
import { DemoPage } from './pages/DemoPage';
import { LoginPage } from './pages/LoginPage';
import { RegisterPage } from './pages/RegisterPage';

export default function App() {
  return (
    <BrowserRouter>
      <div className="min-h-screen bg-surface-primary text-text-primary font-body">
        <Routes>
          <Route path="/" element={<HomePage />} />
          <Route path="/demo" element={<DemoPage />} />
          <Route path="/login" element={<LoginPage />} />
          <Route path="/register" element={<RegisterPage />} />
        </Routes>
      </div>
    </BrowserRouter>
  );
}
```

3. **`apps/web/src/pages/HomePage.tsx`** — Landing page shell:
```tsx
import { Link } from 'react-router';

export function HomePage() {
  return (
    <div className="flex flex-col items-center justify-center min-h-screen">
      <h1 className="text-4xl font-display font-bold text-accent mb-4">FlowBoard</h1>
      <p className="text-text-secondary mb-8">Real-time collaborative Kanban board</p>
      <div className="flex gap-4">
        <Link to="/demo" className="px-6 py-3 bg-accent text-surface-primary font-semibold rounded-lg hover:bg-accent-hover transition-colors">
          Try Demo
        </Link>
        <Link to="/login" className="px-6 py-3 border border-accent text-accent rounded-lg hover:bg-accent/10 transition-colors">
          Sign In
        </Link>
      </div>
    </div>
  );
}
```

4. **`apps/web/src/pages/DemoPage.tsx`** — Demo page shell (will be wired to guest JWT in Plan 06):
```tsx
export function DemoPage() {
  return (
    <div className="flex items-center justify-center min-h-screen">
      <div className="text-center">
        <h2 className="text-2xl font-display font-bold text-accent mb-2">Demo Board</h2>
        <p className="text-text-secondary">Loading collaborative board...</p>
      </div>
    </div>
  );
}
```

5. **`apps/web/src/pages/LoginPage.tsx`** — Login form shell:
```tsx
export function LoginPage() {
  return (
    <div className="flex items-center justify-center min-h-screen">
      <div className="w-full max-w-md p-8 bg-surface-secondary rounded-xl">
        <h2 className="text-2xl font-display font-bold text-text-primary mb-6">Sign In</h2>
        <p className="text-text-secondary">Auth form coming in Plan 05</p>
      </div>
    </div>
  );
}
```

6. **`apps/web/src/pages/RegisterPage.tsx`** — Register form shell:
```tsx
export function RegisterPage() {
  return (
    <div className="flex items-center justify-center min-h-screen">
      <div className="w-full max-w-md p-8 bg-surface-secondary rounded-xl">
        <h2 className="text-2xl font-display font-bold text-text-primary mb-6">Create Account</h2>
        <p className="text-text-secondary">Registration form coming in Plan 05</p>
      </div>
    </div>
  );
}
```
  </action>
  <verify>
    <automated>ls apps/web/src/pages/DemoPage.tsx apps/web/src/pages/HomePage.tsx apps/web/src/pages/LoginPage.tsx apps/web/src/pages/RegisterPage.tsx apps/web/src/App.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/App.tsx` contains `BrowserRouter` and routes for `/`, `/demo`, `/login`, `/register`
    - `apps/web/src/App.tsx` imports from `react-router` (NOT `react-router-dom` — v7 uses `react-router`)
    - `apps/web/src/app.css` contains `@import "tailwindcss"` and `@theme` directive with `--color-surface-primary` and `--color-accent`
    - `apps/web/src/pages/DemoPage.tsx` exports `DemoPage` function component
    - `apps/web/src/pages/HomePage.tsx` contains `Link to="/demo"` and `Link to="/login"`
    - All page components exist and export named functions
  </acceptance_criteria>
  <done>React Router configured with 4 routes. Tailwind v4 CSS-first config with DESIGN.md dark theme tokens. All page shells created.</done>
</task>

<task type="auto">
  <name>Task 2: Create API helper utility for fetch calls</name>
  <files>apps/web/src/lib/api.ts</files>
  <read_first>
    apps/web/vite.config.ts (verify proxy routes to confirm API base URL)
  </read_first>
  <action>
Create `apps/web/src/lib/api.ts` — a thin fetch wrapper that:

1. Uses relative URLs (Vite proxy handles forwarding to backend)
2. Automatically includes `Content-Type: application/json`
3. Supports sending Bearer token from in-memory store
4. Handles response parsing (JSON or throws on error)
5. Provides typed helper functions: `apiGet<T>(path)`, `apiPost<T>(path, body)`, `apiPut<T>(path, body)`, `apiDelete<T>(path)`

```typescript
let accessToken: string | null = null;

export function setAccessToken(token: string | null) {
  accessToken = token;
}

export function getAccessToken(): string | null {
  return accessToken;
}

async function apiFetch<T>(path: string, options: RequestInit = {}): Promise<T> {
  const headers: Record<string, string> = {
    'Content-Type': 'application/json',
    ...((options.headers as Record<string, string>) || {}),
  };

  if (accessToken) {
    headers['Authorization'] = `Bearer ${accessToken}`;
  }

  const response = await fetch(path, {
    ...options,
    headers,
    credentials: 'include', // Send cookies for refresh token
  });

  if (!response.ok) {
    const error = await response.json().catch(() => ({ message: response.statusText }));
    throw new Error(error.message || `API error: ${response.status}`);
  }

  return response.json();
}

export function apiGet<T>(path: string): Promise<T> {
  return apiFetch<T>(path, { method: 'GET' });
}

export function apiPost<T>(path: string, body?: unknown): Promise<T> {
  return apiFetch<T>(path, { method: 'POST', body: body ? JSON.stringify(body) : undefined });
}

export function apiPut<T>(path: string, body?: unknown): Promise<T> {
  return apiFetch<T>(path, { method: 'PUT', body: body ? JSON.stringify(body) : undefined });
}

export function apiDelete<T>(path: string): Promise<T> {
  return apiFetch<T>(path, { method: 'DELETE' });
}
```

This stores the access token in memory per D-04 (NOT localStorage). The `credentials: 'include'` ensures HTTP-only refresh token cookies are sent with requests.
  </action>
  <verify>
    <automated>ls apps/web/src/lib/api.ts && grep -c 'export function' apps/web/src/lib/api.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/src/lib/api.ts` contains `let accessToken: string | null = null` (in-memory per D-04)
    - `apps/web/src/lib/api.ts` contains `credentials: 'include'` (for HTTP-only cookies per D-04)
    - `apps/web/src/lib/api.ts` exports `apiGet`, `apiPost`, `apiPut`, `apiDelete`, `setAccessToken`, `getAccessToken`
    - `apps/web/src/lib/api.ts` contains `Authorization.*Bearer` header logic
  </acceptance_criteria>
  <done>API helper utility created with in-memory token storage, typed fetch functions, and cookie credential inclusion for refresh tokens.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend → Backend API | Fetch calls through Vite proxy; auth via Bearer token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | I (Information Disclosure) | Access token storage | mitigate | Token stored in JS memory variable (not localStorage per D-04), not accessible to XSS attacks reading storage |
| T-01-03-02 | S (Spoofing) | Cookie credentials | mitigate | `credentials: 'include'` sends HTTP-only cookies; cookies set with SameSite=Lax per D-04 |
</threat_model>

<verification>
1. `pnpm --filter @flowboard/web dev` starts Vite on port 5173
2. Navigating to `http://localhost:5173/` shows FlowBoard landing page
3. Navigating to `/demo` shows demo board placeholder
4. Navigating to `/login` and `/register` show form shells
5. Tailwind classes render (dark background, cyan accent visible)
</verification>

<success_criteria>
Frontend shell complete with React Router, Tailwind v4 dark theme, all Phase 1 routes, and API helper utility.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-03-SUMMARY.md`
</output>
